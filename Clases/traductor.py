traductor = {}
palabras_traducidas = input('Ingrese la o las palabras que desee utilizar junto a su traduccion para crear la frase, separado cada par por comas: ')
resultado = palabras_traducidas.split(',')